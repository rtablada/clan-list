import { intAdd } from '../../../helpers/int-add';
import { module, test } from 'qunit';

module('Unit | Helper | int add');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = intAdd(42);
  assert.ok(result);
});
