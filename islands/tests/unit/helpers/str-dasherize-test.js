import { strDasherize } from '../../../helpers/str-dasherize';
import { module, test } from 'qunit';

module('Unit | Helper | str dasherize');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = strDasherize(42);
  assert.ok(result);
});
